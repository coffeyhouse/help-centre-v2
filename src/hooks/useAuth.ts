/**
 * useAuth - Hook for accessing authentication context
 *
 * Returns the current user and authentication functions
 */

// Re-export the useAuth hook from the context for convenience
export { useAuth } from '../context/AuthContext';
